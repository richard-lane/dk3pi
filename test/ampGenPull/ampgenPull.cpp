/*
 * Read in two ROOT files d.root and dBar.root, take their ratios of decay times
 */
void ampgenFit()
{
    // Usage: ampgenPull.exe <Dbar file> <D file>

    // Read in ROOT files

    // Take ratios

    // Perform a fit

    // Save the fit parameters and errors to a text file
}

int main()
{
    ampgenFit();
    return 0;
}
